NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Login del panel admin (/admin)
ADMIN_USERNAME=Cerrotta
# Generar con: node -e "const c=require('crypto');const s=c.randomBytes(16);console.log(s.toString('hex')+':'+c.scryptSync(process.argv[1],s,64).toString('hex'))" 'LA_CONTRASEÑA'
ADMIN_PASSWORD_HASH=salt-hex:hash-hex
# Secreto para firmar la cookie de sesión (mín. 32 caracteres): node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
AUTH_SECRET=
